# HR 离开托管助手需求文档

版本：V1.0  
更新时间：2026-08-29

## 1. 产品定位

本产品不是完整 ATS，也不负责自动寻找、筛选或邀约候选人。唯一核心场景是：HR 暂时离开且无法及时查看 BOSS 招聘消息时，系统识别长时间未获人工回复的候选人来信，发送一条安全的接待回复，并把会话留给 HR 返回后继续处理。

## 2. 核心用户故事

作为 HR，我希望离开工位、开会、午休或下班时开启托管。候选人发来新消息后，如果我在设定时间内没有回复，系统应确认会话仍需处理，再使用固定模板自动回复；我回来后可以关闭托管并查看哪些会话仍需要跟进。

## 3. V1 核心闭环

1. HR 在独立 Chrome Profile 中正常登录一个 BOSS 招聘账号。
2. HR 将该账号与系统连接，并确认连接、登录和页面识别状态正常。
3. HR 开启“离开托管”，设置托管结束时间、无人回复超时和接待模板。
4. 候选人来信后，系统记录最后来信时间并等待超时。
5. 发送前再次确认：托管仍开启、最新消息仍来自候选人、HR 未人工回复、会话未被人工接管。
6. 系统继续校验发送窗口、日上限、最小间隔、失败阈值、浏览器心跳、登录状态和页面风险状态。
7. 条件全部满足后，浏览器伴随端发送回复并确认页面出现发送结果。
8. 系统记录成功、失败、跳过或结果不确定状态；HR 返回后查看待跟进会话并关闭托管。

## 4. 回复策略

V1 默认使用固定模板，目标是确认“消息已收到”，而不是代替 HR 完成招聘沟通。

默认模板示例：

> 您好，消息已经收到。招聘同事当前暂时不在线，稍后会尽快回复您，感谢理解。

模板允许使用职位名称等低风险变量。薪资承诺、录用结论、面试结果、隐私信息和公司政策不得由 V1 自动回答。

AI 回复属于 V1.1 可选能力：只能在受限知识和话术范围内选择或改写模板；不确定、敏感或超出范围的问题必须转人工。AI 不得成为发送链路的单点依赖。

## 5. 核心功能

- 托管控制：按账号开启、关闭和设置自动结束时间；到期默认停止。
- 超时检测：按账号设置 5–120 分钟，发送前必须二次确认 HR 仍未回复。
- 模板管理：固定模板、变量预览、测试预览和账号级绑定。
- 多账号隔离：每个账号使用独立浏览器 Profile、连接状态、模板、额度和托管状态。
- 待跟进会话：展示已自动接待但仍需 HR 继续处理的会话。
- 发送记录：记录触发依据、结果、跳过原因、执行账号和时间。
- 安全控制：发送时段、每日上限、最小间隔、连续失败暂停、验证码/登录失效/DOM 异常停机、紧急停止。
- 管理能力：最小角色权限、关键操作审计、运行诊断和数据范围隔离。

## 6. 浏览器伴随端边界

无官方消息 API 时，必须有一个运行在已登录招聘页面旁边的本地执行端。当前采用 Chrome 扩展，但产品界面统一称为“BOSS 账号连接”，不要求普通 HR 理解配对码、DOM、心跳或发送租约。

伴随端只在 HR 主动登录的可见页面工作；不收集 BOSS 密码，不导出或上传 Cookie/Token，不调用网页私有接口，不绕过验证码、风险提示或平台限制。任何安全策略只能降低风险，不能承诺账号绝不受限。

## 7. 不属于 V1 的范围

- 自动寻找、批量触达或主动打招呼。
- 完整 ATS、人才库和招聘漏斗管理。
- CSV/XLSX 候选人导入。
- AI 简历筛选、岗位解析和自动招聘任务。
- 面试排期、Offer、入职和通用 HR 通知平台。
- 后端保存 BOSS 密码、模拟登录、Cookie 抓取、验证码绕过或反检测。

职位、组织、用户、审计和运行保障作为支撑配置保留，但不进入普通 HR 的日常主流程。

## 8. V1 验收标准

- HR 能在一分钟内完成“确认账号在线 → 设置返回时间 → 开启托管”。
- HR 在超时前回复时，系统不得自动发送。
- 托管关闭或到期后，新消息不得自动发送。
- 同一候选人来信在并发、刷新或重启情况下最多成功发送一次自动回复。
- 验证码、登录失效、页面无法识别、设备离线或后端失联时停止发送并提示人工处理。
- HR 能明确看到托管中的账号、剩余时间、已自动接待数量、失败项和待跟进会话。
- 真实账号必须依次经过 DOM 学习、只监测、草稿、小配额试发后才能启用正式托管。

## 9. 后续迭代顺序

1. V1：固定模板离开托管闭环。
2. V1.1：模板中心、按职位选择模板、托管排班和浏览器通知。
3. V1.2：受限 AI 模板选择/改写、敏感问题识别和人工审核。
4. V2：在取得正式平台能力后替换页面执行端，保留现有策略与审计层。
