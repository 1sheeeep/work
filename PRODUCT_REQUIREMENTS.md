# 候选人消息托管与 AI 简历辅助需求文档

版本：V2.1
更新时间：2026-08-29

## 1. 产品定位

产品服务于多个 BOSS 招聘账号的日常值守，包含两条主链路：

1. **离开托管**：HR 长时间未操作时，系统识别仍未获得人工回复的候选人来信，基于已审核的岗位和公司资料发送一次安全接待回复。
2. **简历辅助分析**：候选人发送简历后，系统提取简历内容并根据对应岗位要求生成结构化分析，供 HR 查看、修正和作出最终决定。

产品不是完整 ATS，不主动搜索、批量触达或自动淘汰候选人。自动回复与 AI 分析均必须可暂停、可追溯、可人工接管。

## 2. 核心用户故事

### 2.1 离开托管

作为 HR，我希望临时离开或下班后，候选人发来消息且长时间没有人工回复时，系统能够确认消息仍需处理，并使用准确的岗位和公司基本信息完成一次接待，避免候选人长时间得不到回应。

### 2.2 简历辅助分析

作为 HR，我希望候选人发送简历后，系统能自动识别简历、提取与岗位相关的事实，指出匹配项、缺失项和需要追问的问题，让我更快完成初步阅读，但不替我作出录用或淘汰决定。

## 3. 自动回复闭环

1. 每个 BOSS 账号在独立 Chrome Profile 中由 HR 手动登录并完成系统连接。
2. 系统只读识别会话列表、未读提示、最新消息方向、消息时间和当前会话。
3. HR 显式开启离开托管并设置自动结束时间。
4. 候选人消息到达后，系统等待账号级超时时间。
5. 发送前再次确认：托管仍有效、最新消息仍来自候选人、HR 尚未回复、会话未人工接管、同一来信没有发送记录。
6. 系统校验浏览器在线、登录正常、页面结构稳定、无验证码或风险提示，并校验发送窗口、配额、最小间隔和失败暂停。
7. 回复内容只能从已审核的公司资料、岗位资料和固定话术组装；事实缺失时使用通用接待语，不允许模型猜测。
8. 浏览器执行端获得单次发送租约后，在可见页面中发送并确认结果；结果不确定时停止且不自动重试。
9. HR 返回后查看自动接待记录和待处理会话，并继续人工沟通。

“未读”只作为候选信号，不能单独触发自动回复。是否发送必须以最新消息方向、人工回复状态、会话版本和幂等记录为准。

## 4. 回复内容和知识来源

### 4.1 可使用信息

- 公司简称、行业、规模、办公地点和经过审核的公司介绍。
- 岗位名称、工作地点、职责摘要、硬性要求、招聘状态和经过审核的薪资展示口径。
- HR 配置的接待话术、回复时效说明和下一步沟通方式。

### 4.2 禁止自动生成的信息

- 录用、面试通过或薪资承诺。
- 未经确认的福利、奖金、社保、公积金、加班和晋升政策。
- 候选人隐私、其他候选人信息或企业内部信息。
- 模型推测的岗位、公司或政策事实。

### 4.3 回复策略

V2 首先采用结构化固定模板，而不是开放式 AI 对话。示例：

> 您好，已收到您关于「{jobTitle}」的消息。该岗位工作地点为 {jobLocation}，主要负责 {jobSummary}。{companyShortName} 是一家 {companyIndustry} 企业。招聘同事当前暂时不在线，稍后会继续与您沟通。

变量缺失时必须删除对应句段或回退到通用接待模板。后续 AI 只能在已审核事实范围内选择、压缩或改写话术；敏感问题、开放问题和低置信度结果必须转人工。

## 5. 简历接收与 AI 分析闭环

1. 系统识别候选人是否发送了简历卡片、附件或平台在线简历入口。
2. 测试阶段只识别“有简历”事件；下载或读取文件前必须确认企业授权、候选人隐私告知和平台允许范围。
3. 文件进入隔离处理区，完成类型、大小、扩展名、恶意文件和重复文件检查。
4. 文本提取层支持 PDF、DOCX 和图片 OCR；原文件与提取文本使用不同权限和保留期限。
5. 系统提取姓名脱敏标识、工作年限、教育经历、技能、项目、最近经历和时间线等结构化事实。
6. 根据会话对应岗位的要求执行分析，输出匹配证据、缺失信息、风险提示和建议追问。
7. 每一条 AI 判断必须关联简历原文证据位置；无法找到证据时标记“未发现”，不得推断为“不具备”。
8. HR 查看分析、修正结论并记录人工反馈；AI 结果不直接改变候选人状态，也不自动发送给候选人。

## 6. AI 简历分析输出

- 基本摘要：工作年限、最近岗位、教育背景和核心技能。
- 硬性条件核对：满足、未发现证据、明显不满足、需要人工确认。
- 岗位匹配点：逐条列出岗位要求、简历证据和证据位置。
- 风险与异常：时间线冲突、表述不清、经验缺口，但不得对年龄、性别、民族、婚育、健康等敏感属性进行推断或评分。
- 建议追问：最多 3–5 个与岗位直接相关的问题。
- 辅助等级：建议优先查看、建议正常查看、信息不足；不提供“自动淘汰”。
- 可追溯信息：模型、提示版本、岗位版本、分析时间和 HR 修改记录。

简历中的文字属于不可信输入。模型不得执行简历中包含的指令，不得把简历内容当作系统提示，也不得调用发送、删除或外部操作工具。

## 7. 核心功能

- 多账号连接：独立 Profile、设备、心跳、页面状态和故障隔离。
- 托管控制：按账号开启、关闭和自动到期。
- 超时判断：未读提示加消息方向、时间、人工回复和幂等复核。
- 公司知识卡：每企业一份已审核资料，支持版本和启停。
- 岗位知识卡：每岗位一份已审核资料，与 BOSS 账号和会话对应。
- 安全回复组装：只引用已审核字段，支持缺失字段回退和测试预览。
- 简历发现：识别简历事件、来源、候选人和岗位上下文。
- 文档处理：隔离存储、病毒检查、文本提取、OCR、去重和删除策略。
- AI 辅助分析：结构化结果、证据引用、人工反馈和重新分析。
- 待处理中心：区分待回复、已自动接待、收到简历、待看分析和需要人工处理。
- 审计与安全：权限、脱敏、模型调用记录、发送记录和数据保留。

## 8. 角色和权限

- 招聘专员：连接授权账号、开启/关闭托管、查看会话、查看简历分析和提交人工反馈。
- 招聘管理员：维护公司/岗位知识、回复模板、AI 分析规则和数据保留期限。
- 系统管理员：管理用户、审计、模型供应商、密钥、运行状态和全局测试模式。

任何角色都不能在真实账号只监测阶段解除服务端发送阻断。进入试发必须通过独立上线门禁和环境配置变更。

## 9. 数据与隐私要求

- 不收集或上传 BOSS 密码、Cookie、Token 和浏览器 Profile。
- 默认不保存聊天正文；确需保存时必须有明确用途、权限和保留期限。
- 简历属于高敏感个人数据，应加密存储、最小权限访问、下载审计并支持到期删除或匿名化。
- 发送给模型前进行最小化和必要脱敏；不得用于供应商训练，并配置企业级数据处理协议。
- 默认禁止把联系方式、证件号码、家庭住址等无关信息发送给模型。
- AI 分析和 HR 决策分开保存，保留人工覆盖记录。

## 10. 浏览器执行端边界

优先保持统一的 Browser Companion 协议，底层可以是 Chrome 扩展或独立实现的本机 CDP 只读探针。两者必须输出统一的页面诊断和会话快照。

执行端只在 HR 主动登录的可见页面工作；不导出 Cookie，不调用网页私有接口，不使用无头浏览器规避检测，不伪造指纹，不绕过验证码或风险提示。参考其他项目时只借鉴公开架构思想，不复制 GPL 源码、选择器或页面脚本。

## 11. 当前不做事项

- 主动寻找、批量打招呼或批量触达候选人。
- 自动淘汰、自动录用或自动安排面试。
- AI 自主决定并发送开放式招聘答复。
- 自动接受联系方式交换、索要简历或标记不合适。
- Cookie 导出、私有接口调用、验证码处理、反检测和无头值守。
- 把第三方 GPL 项目源码或紧密衍生代码直接集成到当前仓库。

## 12. 分阶段验收

### 阶段 A：真实账号只监测

- 服务端和浏览器执行端均硬性禁止发送。
- 正确识别会话列表、未读状态、最新消息方向和时间。
- 页面切换、弹窗、登录失效、验证码和 DOM 变化可诊断并安全暂停。
- 多账号在独立 Profile 中互不影响。

### 阶段 B：公司与岗位知识

- 管理员可以维护和审核公司、岗位资料。
- 回复预览只出现已审核事实，字段缺失不会产生虚构内容。
- 会话能够确定对应账号和岗位；无法确定时只能使用通用模板。

### 阶段 C：简历发现与解析

- 能识别简历事件并关联候选人、岗位和会话。
- PDF、DOCX、图片文本提取成功率达到试运行目标。
- 文件重复、异常、病毒检测失败时不进入 AI。

### 阶段 D：AI 辅助分析

- 输出结构符合固定 Schema，每项判断包含证据或“未发现”。
- 不使用受保护属性，不自动淘汰，不向候选人发送分析结果。
- HR 能反馈、修正和重新分析，所有版本可审计。

### 阶段 E：草稿与受控试发

- 自动回复先生成草稿，由 HR 对照真实会话验证。
- 通过只监测和草稿验收后，才允许测试账号、测试会话、极低配额试发。
- 超时前人工回复、托管到期、人工接管、风险提示和结果不确定均不得发送。

当前实现门禁：后台批准仅表示草稿内容通过人工确认，不等于获得页面填写或发送权限。浏览器执行任务还必须重新验证账号、会话摘要、最新消息方向、页面风险状态和短时租约。

## 13. 实施顺序

1. V2.0：真实账号只监测与 DOM 适配。
2. V2.1：公司/岗位知识卡与安全回复预览。
3. V2.2：简历事件识别、文档隔离和文本提取。
4. V2.3：AI 简历辅助分析、证据引用和人工反馈。
5. V2.4：自动回复草稿联调。
6. V2.5：完成授权与上线门禁后进行单账号小配额试发。
