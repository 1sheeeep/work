# 公司内部招聘值守与 AI 简历助手需求文档

版本：V3.1
更新时间：2026-08-30

## 1. 产品目标

本系统只服务公司内部 HR，帮助多个 BOSS 招聘账号完成日常值守和初筛辅助。它不是招聘平台接口客户端、完整 ATS 或批量获客工具。

系统要解决的核心问题：

1. HR 下班、开会或临时离开时，候选人来信不会长期无人回应。
2. 自动接待只能使用企业和岗位已确认的信息，不能作出招聘承诺。
3. 简历和联系方式相关操作在平台允许范围内完成，并交给 HR 做最终判断。
4. AI 帮 HR 快速阅读简历、找到证据和生成追问，不代替 HR 录用或淘汰候选人。

## 2. 使用对象与范围

- **招聘专员**：启动或暂停自己负责账号的托管，查看待处理会话、简历摘要和异常。
- **招聘管理员**：维护企业资料、岗位资料、值守时段、自动接待模板、联系方式策略和 AI 分析规则。
- **系统管理员**：管理用户、账号、运行保障、审计、数据保留和全局安全开关。

系统仅支持内部配置的企业和 BOSS 招聘账号。当前为单一内部组织使用，前端不展示多租户或复杂集团运营功能。

## 3. 总体架构

```text
招聘管理后台
  ├─ 账号/托管策略     ├─ 岗位与企业资料
  ├─ 待处理消息        ├─ 简历 AI 分析
  └─ 操作记录与异常    └─ 权限与审计
              │
              │ 仅由已配对的本机桥接器主动连接
              ▼
最小 BOSS 浏览器桥接（Chrome Manifest V3）
  ├─ 每账号独立 Chrome Profile
  ├─ HR 手动登录和处理验证码
  ├─ 只读识别会话、岗位和最后消息方向
  └─ 页面变化或验证风险即暂停
              │
              ▼
          BOSS 招聘网页
```

真实页面验收已确认 CDP 附着会与 BOSS 页面重定向同时出现，因此 CDP 不再承担真实页面观测，仅保留离线 fixture 和历史诊断用途。生产方向改用极小 Manifest V3 桥接器，直接在 HR 已打开的页面中只读识别 DOM；它不申请 Cookie、网络拦截、下载或 debugger 权限。系统不保存或传输 BOSS 密码、Cookie、Token 或 Chrome Profile。

## 4. 多账号连接与运行要求

1. 一个 BOSS 账号对应一个账号配置、一个 Chrome Profile、一份桥接器配对和一个本地设备凭据。
2. 同一电脑可启动多个账号；一个账号掉线、风险提示或登录失效时，只暂停该账号。
3. HR 必须在对应 Profile 中手动登录 BOSS；验证码、短信验证、风控验证均由 HR 自己处理。
4. 后台生成一次性连接令牌，桥接器完成配对后只在该 Chrome Profile 的扩展存储中保留本系统设备凭据。
5. 桥接器定期上报在线、暂停、风险、登录失效和页面适配状态；会话身份、预览和最后消息在页面内先转换为 SHA-256 摘要，不上传候选人姓名、消息正文、Cookie 或原始会话 ID。
6. 每个账号可独立启用/停用，不能因一个账号失败影响其他账号。
7. 正式账号只允许 `LOCAL_CDP_CONNECTOR`，不得在正式代码和管理页面保留 Mock 账号类型或 Mock 能力检查。
8. 管理前端、后端与桥接器继续复用 `/api/local-connector/**` 设备信任、心跳、未读观测和岗位去重契约；旧插件的选择器学习界面、DOM 导出、独立超时队列和发送租约不恢复。
9. 候选人工作台中的会话记录只允许来自本地连接器及后端受控链路；前端不得提供伪造候选人来信、手工创建外发草稿或绕过连接器直接发送的入口。
10. 本地连接器识别到的真实岗位标题应自动进入岗位资料：按企业、BOSS 账号和规范化标题摘要去重；新标题仅创建待补全草稿，重复标题只更新观测次数；未补全并经 HR 核对前不得启用，也不得用于包含岗位事实的回复。
10.1. HR 可在已登录的 BOSS“职位管理”页手动触发当前可见岗位同步。桥接器只读取具备稳定 DOM 标识的岗位名称、地点、薪资、经验、学历和可见职位描述；原始平台 ID 必须在页面内摘要化，禁止上传 URL、Cookie 或账号凭据。后端同时按账号、来源摘要和规范化标题去重，只允许更新未核对的导入草稿，不得覆盖人工岗位、已审核采集或启用岗位；不自动翻页、点击、编辑或改变平台招聘状态。
10.2. 页面同步后的岗位审核应尽量减少 HR 重复录入：系统预填已采集的地点、薪资、经验和学历，系统占位描述不得冒充真实职位详情；候选人可见简介可由这些公开字段生成可编辑初稿，但必须由 HR 补充真实职责、逐项核对并明确批准后才能启用。页面同步次数不得在界面上误称为未读候选人数量。
10.3. HR 手动打开 BOSS 职位详情编辑页后，可用同一个只读同步入口采集当前岗位详情。前端严格按 BOSS 页面展示且只展示：公司、招聘类型、职位名称、职位描述、职位类型、是否驻外、经验、学历、薪资详情、职位关键词、工作地址。后端可为校验拆分薪资上下限和薪数，但不得把拆分字段作为 BOSS 字段展示；页面查询参数与平台职位 ID 只能在浏览器内摘要化。系统不得自动点击编辑、保存、发布、删除、关闭或返回控件，详情只允许更新同账号下尚未人工核对的岗位草稿。
11. 页面能力统一划分为未读列表读取、当前会话读取、岗位读取、发送消息、索要简历、交换微信和交换电话，并记录 `UNVERIFIED → READ_ONLY_VERIFIED → READY_FOR_MANUAL_TEST → PRODUCTION_APPROVED` 验收状态。没有真实页面验收的写操作只能登记为受控任务，禁止下发浏览器、自动点击或自动重试。
12. 每个已配对设备必须持久化客户端类型和版本。真实页面心跳、未读观测和会话详情接口只接受 `BROWSER_READONLY_BRIDGE`；历史 `LEGACY_CDP_CONNECTOR` 即使仍持有未过期设备令牌也必须拒绝访问。
13. BOSS 会话列表可能因虚拟滚动、筛选或懒加载只呈现部分会话。本次稳定快照中缺席的历史未读不得自动标记为已读或已处理，只能转为“等待页面再次确认”；在它重新出现并完成会话详情复核前，不得进入安全草稿审核或任何页面动作链路。桥接器必须分别展示“本次页面未读”和“后端持续观察队列”，避免误导 HR。
14. 当前会话详情必须与同一轮稳定会话列表中的身份摘要一致，并连续两次得到相同的最后消息摘要、方向和时间；详情快照超过 2 分钟、来自旧设备或不在最新列表时必须拒绝。HR 手动打开会话造成未读角标消失时，不得仅凭角标闭环：最后消息仍为候选人来信则继续待回复，只有稳定确认最后消息为 HR 外发才标记已处理。
15. 最后一条消息可以是文字、图片、表情或其他非文本消息；桥接器只在页面内生成类型与结构摘要，不上传媒体地址或正文，不得因没有文字而静默跳过。插件必须单独展示当前会话详情是“已复核、等待稳定、未选择或解析失败”，列表统计不得冒充详情成功。
16. 只读未回复监测与消息发送权限必须分离：账号已连接且具备读取能力时，可配置 HR 离开时间、超时阈值并生成待审草稿，不要求 `MESSAGE_SEND`。只有开启真实自动发送时才校验发送能力、生产批准和配额；只监测模式下前后端必须固定关闭自动发送开关。
17. “企业/岗位资料 → 安全草稿”必须使用统一资格检查：只允许同一账号下规范化标题唯一且严格匹配的启用岗位；公司知识、公司行业、公司介绍、岗位知识、岗位回复简介和非人工采集资料核对状态均须逐项检查。每次草稿保存资格状态、阻断码、匹配岗位 ID 及公司/岗位知识版本；任一项不满足时整段回退通用收悉文案，禁止混用未审核事实或相似岗位。HR 修改资料后可手动重新评估现有未读草稿，该操作不得访问或操作 BOSS 页面。
18. 安全草稿只能基于严格匹配且已审核的企业/岗位知识批准。批准界面必须展示实际回复正文及企业、岗位知识版本；任一知识版本、匹配岗位、阻断项或草稿正文发生变化时，旧批准自动失效并要求 HR 重新核对。通用回退草稿不得批准，发送动作登记也必须再次校验当前批准与资料版本。
19. 真实未读或岗位页面形成的岗位草稿必须进入独立审核队列，按真实未读观测次数排序并逐岗位处理。系统可将岗位详情保存、页面来源确认、岗位回复知识批准和岗位启用收敛为一次原子操作，但必须要求 HR 分别确认真实页面核对、无夸大知识和启用决定；企业知识未由系统管理员审核、存在占位内容或同账号已有规范化同名启用岗位时必须整体失败且不保存部分结果。采集岗位不得再通过普通状态接口绕过完整审核；成功后才可重新评估现有未读草稿。
20. 未匹配会话必须按招聘账号和规范化观测岗位标题聚合，禁止跨账号或跨标题批量处理。自动严格匹配失败时，HR 可明确确认一组观测标题对应同账号下某个启用岗位；目标岗位的企业和岗位知识必须完整审核，确认请求同时携带当前源标题、目标标题和会话集合，任一记录状态变化时整组失败。人工关联需保存标题快照和审计，后续观测标题变化、岗位关闭或账号变化时自动失效并恢复严格匹配；该操作只重算后台草稿，不访问 BOSS 页面。

## 5. 挂机值守与自动接待

### 5.1 策略配置

每个账号配置以下规则：

- 托管开关和自动结束时间；
- 生效日期、工作日和时间段；
- 候选人来信后等待多久才可处理；
- 每日自动回复上限和两次发送最小间隔；
- 连续失败后暂停时长；
- 是否允许自动求简历、自动接收简历、平台内交换联系方式；
- 本机紧急停止和全局只监测开关。

### 5.2 发送条件

同一会话只有同时满足以下条件才可发送一次接待：

1. 账号托管仍在有效时段内；
2. 会话最新消息来自候选人；
3. HR 尚未在该条消息后手动回复；
4. 已等待到账号的回复阈值；
5. 会话未被人工接管，且不存在本轮已发送记录；
6. Chrome 在线、BOSS 已登录、页面结构已验证且无验证码/风险提示；
7. 未超出日配额和最小间隔；
8. 已取得短时操作租约，发送结果可由页面确认。

只要页面状态异常、会话身份不唯一、结果不确定或平台出现风险提示，系统立即暂停该账号，不猜测、不重复发送。

### 5.3 自动回复内容

回复只能从已审核的资料组成：

- 企业简称、行业、规模、办公地点和审核后的企业介绍；
- 岗位名称、地点、职责摘要、硬性要求、招聘状态和审核后的薪资展示；
- HR 配置的接待话术、离开说明和后续沟通方式。

禁止自动承诺录用、面试、薪资、福利、到岗时间、联系方式或其他未经确认的事实。岗位无法唯一匹配、资料未审核或字段缺失时，必须使用不含岗位承诺的通用接待语。

## 6. 简历和联系方式操作

系统只点击 BOSS 网页中当前可见且平台允许使用的入口；不调用私有接口、不绕过平台限制。

### 6.1 简历

- 识别“附件简历”“在线简历”或其他平台明确展示的简历事件。
- 在账号策略允许、会话状态满足平台规则时，执行“求简历”。
- 对候选人发送的简历，按账号策略进入“待 HR 确认”或自动接受；默认先人工确认。
- 接收后保存来源、时间、文件摘要、处理状态和对应岗位。
- 简历文件进入隔离区，检查文件类型、大小、重复文件和恶意文件；失败时不进入 AI。

### 6.2 微信、电话和其他联系方式

- 仅支持 BOSS 页面明确提供的“交换微信/联系方式”等操作。
- 自动交换默认关闭，必须由管理员对账号显式开启。
- 自动操作只能使用公司统一联系方式；不得自动披露 HR 私人微信或电话。
- 联系方式采用脱敏展示、最小权限和到期删除；只有确有招聘需要的 HR 可查看完整信息。
- 每一次求简历、接受简历、联系方式交换都记录账号、会话匿名键、操作时间、结果和失败原因。

当前迭代新增“HR 主动选择文件 → 本机临时解析 → HR 核对文本 → 明确授权 OpenAI 分析”的内部门禁：接受非加密、无宏、最大 8MB 的 PDF/DOCX；可选 OCR 仅接受 PNG、JPG/JPEG，且必须先启用并通过 ClamAV。文件只进入本机临时处理区，不写入业务数据库或持久卷。扫描未通过、扫描器或 OCR 服务不可用、返回异常时都必须拒绝文本提取与外部发送；提取成功也只能返回给 HR 校对，不得直接调用 OpenAI。需要长期留存的加密隔离区仍须在后续门禁完成后才开放。

## 7. AI 简历辅助分析

1. 支持在企业授权和隐私规则允许下解析 PDF、DOCX、图片 OCR 或平台可见简历。
2. 提取工作年限、教育经历、技术/业务技能、项目、最近经历和时间线等结构化事实。
3. 依据对应岗位的硬性条件、加分项和岗位说明生成分析。
4. 每项结果必须给出简历证据或标记“未发现证据”，不能把“未发现”推断为“不具备”。
5. 输出包括：简历摘要、匹配证据、待确认缺口、风险提示、3–5 个建议追问和“优先查看/正常查看/信息不足”建议。
6. 不得根据年龄、性别、民族、婚育、健康等受保护或敏感属性打分或推断。
7. AI 不自动淘汰、不自动录用、不自动发送分析结果。HR 可追加“作为参考采纳 / HR 已修正 / 本次未采用”的不可覆盖复核记录；重新分析会形成新版本，所有版本与反馈均可追溯。

简历内容属于不可信输入，模型不得执行其中的指令，也不能调用发送、删除或外部操作工具。

### 7.1 外部 OpenAI 处理规则

当前实现允许使用 OpenAI 作为外部分析服务，但必须同时满足：

1. 简历事件已由 HR 标记为“授权 AI”；
2. 每一次调用前，当前 HR 在界面再次确认本次简历文本可以对外发送；
3. 只发送本次岗位判断必要的文本与对应岗位资料；不上传 BOSS Cookie、聊天正文、账号密码或简历文件；
4. 本地不保存被粘贴的简历原文，只保存 SHA-256 输入摘要、结构化分析结果、模型名、时间与审计；
5. 请求使用无会话、`store=false` 的方式；外部服务仍适用其自身的数据处理与保留政策，公司应按内部隐私制度取得必要授权；
6. API Key 只在服务端部署环境提供，禁止进入前端、数据库、日志或 Git；未配置 Key 与模型时功能必须保持关闭。
7. 只允许系统管理员访问“AI 接入”页；页面只能返回 Key 是否配置、模型名、官方端点主机、超时与保留期，不得返回 Key 内容。
8. OpenAI 地址必须为 HTTPS 官方端点；配置就绪检查必须同时验证启用开关、Key、模型和端点，任一缺失时禁止简历分析。
9. 管理员可运行不含候选人、简历、岗位或聊天数据的固定连通性探针；探针同样使用 `store=false`，并记录成功/失败、模型、耗时和请求 ID 的脱敏审计。
10. 前端简历分析请求超时必须长于后端 OpenAI 请求超时，避免后端仍在处理而页面提前误报失败。

### 7.2 AI 结果保留与到期清除

1. 默认只保留 AI 结构化结果、摘要及其 HR 复核 90 天；保留期必须支持由部署配置调整。
2. 到期后自动清除结构化结果、摘要和关联 HR 复核，保留输入 SHA-256 摘要、模型/提示词版本、创建时间和不可变审计记录。
3. 清除行为必须记录时间、对象和原因，但审计不得包含简历原文、OCR 文本或 AI 输出正文。
4. 到期清除不删除候选人主记录、岗位、简历事件来源摘要，也不触发 BOSS 平台操作。
5. 已清除的 AI 结果不能继续追加 HR 复核；界面必须明确显示“已按保留策略清除”。

## 8. HR 前端信息架构

普通 HR 使用按工作目标分组的导航，而不是按技术模块堆叠入口；页面顶栏必须持续展示当前运行边界（例如“只读监测”）。核心入口如下：

| 模块 | 目的 |
| --- | --- |
| 日常工作 | 今日总览、待处理消息、岗位资料和简历分析；始终直接可见 |
| 消息值守 | 值守设置和招聘账号；始终直接可见 |
| 管理中心 | 企业资料、HR 用户、AI 接入、运行保障和操作日志；按权限折叠展示 |

“消息值守”默认只展示“值守设置、待审草稿、处理记录”三个 HR 工作页签。页面能力矩阵、真实页面验收、动作租约、生产批准、紧急停止和离线演练属于管理员高级工具，不得与 HR 日常操作平铺展示；普通招聘专员不可进入高级验收视图。

首页在没有真实账号时只能展示“等待真实账号”的准备清单，不得用虚构企业、岗位或候选人填充。首页只突出当前下一步，完整准备清单默认折叠；同时说明“不保存 Cookie/密码、不自动发送、异常即暂停”三条安全边界。账号页默认只显示准备状态和已保存的内部账号配置；连接码、独立浏览器 Profile 等本机接入细节必须收进“真实账号到位后”的高级区域。历史浏览器心跳失效只能显示“未运行”，不得形成阻断弹窗或暗示平台发生异常。管理员在上述基础上维护账号、企业、岗位、统一联系方式、用户权限、审计和运行保障。

## 9. 安全、隐私与可靠性

- 不保存 BOSS 密码、Cookie、Token、完整 Chrome Profile 或原始会话 ID。
- 默认不保存聊天正文；需要保存的简历和联系方式采用最小范围、最短期限和访问审计。
- 简历和联系方式属于敏感数据，需加密存储、权限隔离、下载审计和到期删除。
- 自动化结果无法确认时标记 `UNKNOWN` 并停止，不自动重试。
- 账号掉线、登录过期、验证码、风险提示、DOM 变化或页面被重定向时，连接器立即暂停。
- 正式连接器设备被重新配对后，新设备第一次成功上传稳定会话快照时，旧设备仍标记未读的记录必须归档为来源已替换，不得继续计入当前待处理数量，也不得误标为 HR 已回复。
- 系统必须记录策略变更、自动发送、简历操作、联系方式交换、AI 调用和人工覆盖。
- 自动化可降低遗漏，但不能保证第三方平台账号绝不会受到限制。

## 10. 不做事项

- 主动搜索候选人、批量打招呼、批量触达或营销式群发；
- 自动淘汰、自动录用、自动安排面试；
- AI 自主发送开放式回复；
- Cookie 导出、私有接口调用、验证码处理、反检测、无头值守；
- 通用浏览器自动化插件、选择器学习 UI、DOM 导出、Cookie/网络请求采集；仅保留本项目内置的最小只读桥接器。
- CSV/Excel 候选人导入、复杂 ATS 流程、多租户运营能力。
- 前端手工创建候选人、手工伪造硬规则或 AI 结论、招聘任务编排、面试排期与独立 HR 通知中心。

## 11. 实施与验收顺序

1. **最小只读桥接基础**：多账号独立 Profile、手动登录、配对、心跳和账号隔离。
2. **只监测**：读取会话列表、未读、最后方向、时间、风险和登录状态；不发送。
3. **岗位与企业知识**：维护审核资料和固定接待模板。
4. **受控自动接待**：单账号、低配额、可见页面、发送确认和异常暂停。
5. **简历与联系方式**：简历事件、受控求/收简历、平台内联系方式交换。
6. **AI 简历分析**：隔离解析、证据化分析、HR 反馈和简洁展示。
7. **多账号试运行**：逐账号启用、观察审计和故障演练。

任何阶段都必须以前一阶段的安全验收通过为前提，不得跳过只监测和受控试发。

HR 的资料办理顺序必须固定为：账号连接、企业公开资料审核、真实岗位逐个核对、安全草稿重新评估。企业资料未审核时不得把岗位审核作为当前首要步骤；每个页面必须明确显示当前阻断项和下一跳入口。企业公开资料只能由公司确认后填写，系统不得根据岗位文本、网页片段或模型推断并自动批准。

真实 BOSS 职位详情进入安全草稿前，系统必须按 BOSS 原字段名称保存和展示上述 11 个字段。筛选要求、手工拆分薪资、岗位简介等内部数据不得混入 BOSS 职位表单；安全草稿所需摘要由已核对真实字段在后台生成。采集值只能作为待核对草稿；HR 审核启用时不得静默丢弃已采集字段，字段语义不可信时必须留空或阻断，不得猜测。企业知识、岗位知识、页面核对和严格账号内标题匹配全部通过后，才允许生成可批准的知识草稿。

岗位资料的唯一业务来源必须是已配对账号在 BOSS“职位管理”页的只读同步，前端岗位数量必须与当前同步的真实岗位一一对应。未读会话中出现的标题仅作严格匹配证据，不得自动新建岗位；历史手工、推断或非 `VISIBLE_PAGE` 岗位不得出现在岗位页和首页统计中。删除历史岗位前必须确认无候选人、AI 分析或会话匹配引用。

## 12. 真实页面单次人工验收门禁

发送消息、索要简历、交换微信和交换电话必须分别建立验收记录，不能因为一项通过而默认其他写操作可用。每项验收至少包含：独立 Chrome Profile 已登录、当前会话身份稳定、页面不存在验证码/风险提示/登录失效、HR 明确确认、动作特有前置条件以及可核对的预期页面回执。

- 没有真实账号和对应页面时，验收状态必须保持 `WAITING_REAL_PAGE`。
- 验收准备阶段只允许生成 `PREVIEW_ONLY` 预览，禁止点击、输入、发送、任务领取或后台轮询执行。
- 会话摘要不稳定、页面类型错误、能力未进入人工测试状态或出现风险提示时，预览也必须拒绝。
- 后续真实页面试验必须由 HR 在场、逐账号、逐动作、单次进行；结果不确定时按失败处理并继续锁定能力。
- 只有留存脱敏证据且人工验收通过后，能力才可进入更高状态；生产批准仍须独立的管理员决策和低配额试运行。
- 页面就绪报告只能由已配对本地连接器提交，且至少连续稳定识别三次；报告只包含会话、页面入口的 SHA-256 摘要和布尔状态，不包含 DOM、候选人信息、消息正文或联系方式。
- 就绪证据超过 2 分钟不得开启测试；人工测试窗口固定为 10 分钟。窗口超时自动记为失败并锁定能力，不自动重试。
- 单次人工测试通过后，能力最多保持 `READY_FOR_MANUAL_TEST`；不得由状态机自动变为 `PRODUCTION_APPROVED`。
- 回复入口就绪检查必须由 HR 在当前已读会话中手动发起，至少连续 3 次识别到同一会话和同一回复控件结构。页面检查只可提交会话与控件 SHA-256 摘要、页面状态和稳定次数，不得读取输入框内容或触发点击、输入、发送事件。

### 12.1 单次动作租约和回执

- 只有状态为 `READY` 的受控任务、状态为 `PRODUCTION_APPROVED` 的对应能力以及运行中的已配对设备同时满足时，后端才可签发动作租约。
- 每个任务最多存在一个租约；租约有效期固定 30 秒，原始令牌只返回一次，数据库只保存 SHA-256 哈希。
- 租约仅包含动作类型、任务标识、不可逆会话摘要和过期时间，不包含消息正文、候选人姓名、联系方式或 Cookie。
- 回执必须明确为 `SUCCEEDED`、`FAILED` 或 `UNKNOWN` 并携带脱敏结果摘要；相同结果和摘要可幂等重放，不同结果不得覆盖。
- 租约超时、回执缺失或页面结果无法确认时，任务必须进入 `UNKNOWN`，当前账号立即冻结，且禁止自动重试。
- 协议框架本身不代表页面执行能力；在真实页面执行器逐动作验收之前，连接器不得轮询或消费动作租约。

### 12.2 生产批准与低配额试运行

- 每个账号的发送消息、索要简历、交换微信和交换电话必须分别批准，不能共享或继承批准。
- 只有对应真实页面人工验收为 `PASSED` 时才能提出申请；申请人与最终批准人必须是两位不同的管理员。
- 小时额度只能设置为 1–5 次，每日额度只能设置为 1–20 次，且小时额度不得高于每日额度；每次签发租约前都重新计算。
- 批准最长有效 168 小时，到期自动转为 `EXPIRED` 并锁定对应页面能力，禁止自动续期。
- 管理员可随时撤销单项批准；账号紧急停止必须一次撤销该设备全部有效批准、停止设备并阻止新租约。
- 双人批准只开放低配额协议门禁，不代表连接器已有页面执行器；执行器未验收时仍不得领取任务。

### 12.3 本地仿真执行器与证据链

- 在真实页面执行器开发前，四类受控动作必须先在与 BOSS 完全隔离的本地 fixture 中通过状态机演练。
- 仿真适配器必须声明 `FIXTURE_ONLY`，执行器清单必须固定 `productionEnabled=false` 且不得包含真实页面选择器集合。
- 每次演练必须留存执行前、执行后和明确回执三份 SHA-256 摘要；目标摘要变化、风险状态、页面状态未变化或适配器尝试点击/输入时一律失败关闭。
- 连续异常必须打开本地熔断器；熔断打开后不能由后续成功自动恢复，只允许显式的本地仿真重置。
- 离线演练结果仅用于验证协议实现，不得改变真实页面能力、人工验收、生产批准、任务状态或租约领取资格。
- 离线故障矩阵至少覆盖目标漂移、风险或验证页、状态不变、浏览器输入企图和步骤超时；任一故障未被阻断时构建必须失败。
- 每个离线检查和仿真步骤必须配置有界超时，禁止因适配器无回执而无限等待。
- 多账号连接器必须维护账号级独立故障计数和冻结状态；任何账号的验证码、登录失效、目标漂移或未知回执不得停止或污染其他账号。
- 冻结账号不得因后续健康心跳自动恢复；人工确认并连续三次稳定只读探测后，只能恢复为监测状态，不能连带恢复生产写能力。
- 账号冻结状态必须写入本机受限权限状态文件并跨进程重启保留；冻结期间主循环只能上报暂停，不得继续读取会话或领取动作。
- 进入真实页面验收前必须运行脱敏综合自检，至少检查传输安全、状态文件权限、账号隔离、配对和冻结状态；自检通过不得解释为生产写能力已批准。
