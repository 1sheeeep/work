# DEV_STATE

更新时间：2026-08-30。当前分支：`main`。

## 当前产品方向

产品已从“Chrome 浏览器插件伴随端”迁移为“本地 BOSS CDP 连接器”。最终范围见 [`PRODUCT_REQUIREMENTS.md`](PRODUCT_REQUIREMENTS.md)。

- 服务对象：公司内部多个 BOSS 招聘账号。
- 核心闭环：离开托管自动接待、平台内简历/联系方式操作、AI 简历辅助分析、HR 简洁工作台。
- 本机方式：每账号独立 Chrome Profile 与 CDP 端口，HR 手动登录和处理验证码。
- 安全底线：不保存 Cookie/密码，不调用私有接口，不使用无头浏览器；异常、风险提示、登录失效和结果不确定时暂停。

## 本次已完成

1. 已删除数据库中明确标记的 E2E、Mock、UI 测试企业、账号、岗位、候选人与关联运行数据，保留正式企业、账号和岗位。
2. 正式账号类型由 `BROWSER_COMPANION` 升级为 `LOCAL_CDP_CONNECTOR`；Flyway V30 会自动迁移既有正式账号。
3. 后台账号、托管和岗位页面改为“本地 Chrome 连接器”文案。
4. 新增 [`boss-local-connector`](boss-local-connector/)：支持多账号配置校验、独立 Profile、独立 CDP 端口、手动登录、一次性连接令牌、本地状态保护和安全心跳。
5. 连接器会通过 CDP 只读检查 BOSS 页面、沟通页、登录失效和风险/验证提示；在沟通页稳定时同步会话未读计数、岗位标题和不可逆摘要。若 HR 已手动打开会话，会额外只读复核最后消息方向与时间，驱动现有安全草稿条件；候选人姓名、消息正文、会话 ID、Cookie 和密码均不上传，也不会点击、跳转、填写输入框或发送消息。
6. 新增内部简历登记台账：仅保存事件摘要、来源标签与人工审核状态；原始文件、路径和 BOSS 页面数据不进入当前系统。批准后才允许进入下一阶段 AI 分析。
7. 已接入 OpenAI 简历辅助分析链路：HR 授权后仍需逐次确认，才发送本次粘贴的简历文本；请求设置 `store=false`，本地仅保存 SHA-256 输入摘要、模型版本、结构化证据/追问结果和审计。未配置 `APP_OPENAI_ENABLED`、`OPENAI_API_KEY` 与 `OPENAI_MODEL` 时调用保持关闭。
8. 已补齐 AI 后人工复核：每次成功分析可追加“采纳、修正或未采用”的 HR 说明，反馈不可覆盖且不会改变候选人状态或触发外发动作。
9. 已支持 PDF/DOCX 一次性本机提取：仅接受非加密、无宏、最大 8MB 文件，校验文件签名、PDF 页数和 DOCX 结构后提取文本，文件只进入本机临时处理区，不写入业务数据库或持久卷；提取成功后仍需 HR 确认才会发送文本给 OpenAI。
10. 已接入可选 ClamAV 流式扫描门禁：启用后，风险文件、扫描服务故障和异常返回均会拒绝处理并写审计；扫描服务只运行在 Docker 内部网络，病毒库卷不保存简历原文件。
11. 已接入可选本机 Tesseract 扫描件 OCR：只支持 PNG、JPG/JPEG，强制经过已启用的病毒扫描；识别文本只返回当前 HR 浏览器供核对和修改，提交 OpenAI 前仍需独立确认。
12. 已接入 AI 分析结果保留策略：默认 90 天后自动清除结构化结果、摘要和关联 HR 复核，保留输入摘要和不可变审计；清除后前端明确展示状态，不会误报为分析失败。
13. 已完成第一阶段前端重构：产品名称统一为“招聘值守台”，侧边栏按“工作台、值守与消息、岗位与人才、组织设置、运行管理”分组；首页重构为状态总览、当前优先事项、安全边界、离开托管和最近处理记录。没有真实账号时只显示接入准备，不填充虚构运行数据。
14. 已收敛“账号与浏览器”页：默认不再展示四步配对向导、连接码和过期心跳告警；已保存账号折叠显示，历史设备状态统一为“未运行”。真实账号到位后，管理员才在高级区域打开一次性本机接入设置。
15. 已完成 OpenAI 服务端接入向导：新增仅系统管理员可见的“AI 接入”页、脱敏配置就绪检查和无候选人数据连通性测试；强制官方 HTTPS 端点、`store=false`、安全错误分类和请求 ID 审计。简历分析前端超时已调整为 75 秒，未就绪时禁止提交并引导管理员配置。
16. 已完成旧插件与 Mock 正式链路清理：后端运行包迁移为 `localconnector`，前端、连接器和安全配置统一使用 `/api/local-connector/**`；删除 Mock 网关/账号类型、插件消息同步、人工会话绑定、岗位采集、草稿填写租约与 DOM 发送租约。候选人工作台同步删除手工伪造来信、创建外发草稿和直接审核发送入口，只读展示由真实链路产生的会话记录。V35/V36 已完成数据迁移、旧表字段清理及剩余连接器表、列和索引命名统一。历史 Flyway 文件及可能包含真实记录的孤立面试通知表暂不做破坏性删除。
17. 已完成真实未读岗位标题入库：连接器实时识别并在后端按“企业 + BOSS 账号 + Unicode/大小写/空白/标点规范化标题”去重；V37 增加唯一来源摘要、最后观测时间和观测次数。历史 28 条真实未读观察已回填为 7 个唯一岗位草稿。草稿仅标题可信，补全并由 HR 核对前不能启用，也不会进入岗位知识回复。
18. 已建立统一页面能力矩阵和受控动作队列：V38 持久化七类页面能力及其真实验收状态；已有真实证据只会自动提升未读列表、会话详情和岗位标题三项只读能力。发送消息、索要简历、交换微信和交换电话在没有真实账号验收时保持 `UNVERIFIED`；HR 可登记任务用于后续逐项测试，但任务固定阻断且不存在浏览器领取/执行接口。
19. 已准备真实页面单次人工验收链路：V39 为每台正式连接器生成“发送消息、索要简历、交换微信、交换电话”四张独立验收卡，记录前置条件、预期回执和失败原因；本地连接器新增严格的 `PREVIEW_ONLY` 动作校验契约。没有真实页面时状态固定为 `WAITING_REAL_PAGE`，系统不提供点击器、任务领取或执行接口。
20. 已完成验收状态机和脱敏证据门禁：V40 只接受已配对本地连接器上报的稳定会话摘要与可见操作入口摘要，连续稳定识别至少三次且无验证码/风险提示后才进入 `PREPARED`。管理员可开启 10 分钟人工窗口并登记明确成功或失败；超时、失败和结果不确定都会自动锁定对应能力。单次通过只保持 `READY_FOR_MANUAL_TEST`，绝不会自动升级为生产批准。

## 下一开发顺序

1. 用真实页面验证连接器的沟通页、登录失效和风险提示探针，以及会话列表的稳定 DOM ID。
2. 用真实页面确认选中会话的最后消息方向与时间只读复核；页面结构变化时保持暂停。
3. 把安全草稿、时段、配额、租约和人工接管整理成 CDP 连接器的明确状态链路；自动发送继续关闭。
4. 单账号小配额受控自动接待。
5. 用真实页面验证简历事件、平台内联系方式交换，再接入受控操作。
6. 由公司管理员按 README 完成 OpenAI 服务端 Key 与模型配置，先通过无简历数据连通性测试，再用脱敏测试文本验证分析输出、版本记录与 HR 复核体验；真实简历只在公司已完成隐私授权后使用。
7. 为人工批准的简历事件评估可选加密隔离区与下载审计；当前原文件不留存，AI 结果已有到期清除，OCR 仅可辅助人工核对。
8. 将受控页面操作逐项纳入真实页面验收；未通过验收的发送能力继续固定拒绝执行。

## 验证要求

- 后端测试、前端测试、连接器配置单元测试必须通过。
- Flyway 必须在本地 Docker PostgreSQL 上迁移至 V40。
- 在没有真实账号页面适配通过前，`APP_BROWSER_MONITOR_ONLY` 必须保持 `true`，连接器不实现发送动作。
