# DEV_STATE

更新时间：2026-08-29。当前分支为 `main`。

## 当前产品边界

产品需求已升级为 V2 双主链路：HR 离开期间的超时消息安全接待，以及候选人发送简历后的 AI 辅助分析。当前运行版已完成托管基础能力和公司/岗位回复知识卡，并处于真实账号只监测阶段；简历解析和 AI 分析尚待按路线图实现。

1. Session 登录、CSRF、角色、企业数据范围和 HR 用户管理。
2. BOSS 账号、职位、候选人、筛选留痕与会话消息。
3. 候选人最新来信超时后生成草稿或自动回复；V18 已增加在岗、临时离开、下班托管、开始时间和自动结束时间。
4. 每个 BOSS 账号独立配置超时、时段、日配额、最小间隔、失败暂停和模板。
5. Chrome Manifest V3 伴随端通过一次性配对码绑定 BOSS 账号，上报心跳并同步脱敏会话消息。
6. 设备令牌和原始网页会话 ID 只保存 SHA-256 摘要；每账号最多一台活跃设备。
7. 浏览器伴随端从后端读取账号策略，未配对或后端不可达时失败关闭。
8. 已绑定网页会话排除后端 Mock 发送，避免双通道重复回复。
9. 人工接管、时段/配额/频率限制、发送前二次复核、连续失败暂停和租约幂等仍保留。
10. 关键操作审计、请求 ID、日志脱敏、审计表只追加、Gateway 弹性和 Prometheus 监控仍保留。
11. 招聘专员可在自己授权账号上快速开启 1/2 小时、下班托管或点击“我已返回”；完整模板和限流策略仍只允许管理员修改。
12. 托管到期由调度扫描、后端处理和浏览器发送申请三层同时阻断，不依赖前端页面保持打开。
13. 候选人列表已收敛为待跟进会话中心，接口返回最后消息摘要/方向/时间、待审核草稿、最近自动接待状态和是否仍需 HR 跟进；自动接待成功不等同于 HR 已完成跟进。
14. 首页已重构为离开托管控制台，集中提供按账号快速托管、全部返回、待跟进数量、连接异常和最近自动接待记录；招聘管理指标不再占据普通 HR 首页。
15. 浏览器伴随端 V0.4 增加连续稳定快照、可见风险提示扫描、输入框/按钮可交互检查和弹窗遮挡停机；扩展弹窗改为四步托管准备检查。
16. BOSS 账号页已提供面向普通 HR 的四步连接向导：准备专用 Chrome、生成一次性配对码、识别招聘会话页面、只监测验证；招聘专员可为授权账号配对设备，账号资料和高级配置仍仅限管理员。
17. 普通 HR 导航已收敛为托管首页、待处理消息、账号连接、回复设置与记录；托管设置页移除重复的首页总览和设备入口，限流、失败暂停、租约与审计继续作为后台默认保护。
18. 真实账号试运行阶段已默认关闭后台自动回复调度，并增加服务端 `app.browser.monitor-only=true` 与扩展 V0.5 `monitorOnly=true` 双重发送阻断；此阶段只允许脱敏页面诊断和消息状态同步。
19. V2 需求已定义公司/岗位已审核知识、安全回复组装、简历事件发现、隔离解析、带原文证据的 AI 辅助分析和 HR 反馈闭环；AI 不自动淘汰、录用或向候选人发送分析结论。
20. V19 增加公司与岗位回复知识、审核状态和独立版本；组织与职位页面可维护知识，职位页面可生成安全预览。渲染器只使用双方均已审核的资料，任何必填信息缺失时整段回退到通用接待语，且不进入真实发送链路。
21. 浏览器伴随端 V0.16 已完成真实 BOSS 左侧列表、未读状态及选中会话最后消息方向识别，并把会话/消息不可逆摘要、方向和时间同步到后端；不上传候选人姓名和消息正文。
22. V21–V22 已建立安全草稿与服务端资格状态机。只有离开托管有效、曾出现未读、选中详情在 3 分钟内复核、最新方向为候选人且达到账号阈值，才进入待人工确认；HR 可批准、拒绝或人工接管，批准仍不会写入 BOSS 或发送。
23. V23 增加正式 `BROWSER_COMPANION` 账号类型，创建界面默认推荐浏览器招聘账号；正式账号通过独立 Chrome Profile 和一次性配对连接，配对成功后自动确认设备能力，不再复用 E2E Mock 账号承载真实观察数据。
24. V0.17 / V24 已实现只填写不发送的受控草稿执行：后台批准草稿进入 READY，服务端在账号、托管、最新候选人消息和三分钟详情复核全部满足时签发 120 秒租约；扩展只对 HR 当前选中的空输入框填写并回执，页面变化或结果不确定时清空并停止，永不点击发送。`APP_BROWSER_DRAFT_FILL_ENABLED` 和扩展本机开关默认均关闭。
25. 无人值守加固已完成：后台安全看门狗识别 2 分钟心跳超时，按账号隔离标记设备离线；45 秒发送租约和 120 秒草稿租约过期后标记 `UNKNOWN` 且不重试。Chrome 重启会清除旧主标签租约并安全暂停，草稿填写也纳入单标签互斥。
26. V25 已实现安全草稿的 HR 手动发送闭环：页面识别最新消息为 HR 外发后记录处理类型和时间，前端将其从待处理队列移入“最近已处理”。新的候选人来信会清理上一周期的审核、详情和填写状态，必须重新走资格校验。
27. V26 已将正式浏览器招聘账号与 Mock 完全隔离：配对、设备认证、会话绑定、托管配置、执行记录和未读观察仅对 `BROWSER_COMPANION` 开放。迁移会撤销历史 Mock 设备、作废配对码并关闭 Mock 托管，历史数据保留但不出现在 HR 运行界面。
28. V27 修正历史 `gateway_type VARCHAR(16)` 无法容纳 `BROWSER_COMPANION` 的字段长度遗漏，数据库与 JPA 统一扩展为 24 字符，正式账号可正常创建。
29. 安全草稿链路已收敛为单一生成器：职位预览与真实未读草稿使用完全相同的审核、缺失字段和回退规则；岗位匹配允许全半角、大小写、空白与标点等展示差异，但不做包含或相似度猜测，出现零个或多个匹配时一律使用通用回退。职位页直接展示“安全草稿就绪”和缺失资料，新建职位优先选择正式浏览器账号所属企业。
30. 浏览器伴随端 V0.18.1 增加当前岗位快速采集：HR 点击入口时先从正在查看的岗位详情页读取可见字段，再打开独立核对页并自动填写；以配对账号自动确定企业与账号并创建职位草稿。同账号同名草稿/启用岗位不会重复创建，采集不记录页面 URL、Cookie 或登录信息，也不会自动启用和审核。
31. 托管首页增加“真实账号接入准备”检查：只按正式浏览器账号、已配对设备、真实关联岗位、企业知识审核与岗位安全草稿就绪状态计算四步进度；未具备真实账号或岗位时明确显示等待项和下一步入口，不用 Mock 或预设数据冒充完成状态。
32. 新增本机岗位采集实验台：扩展可对 `localhost:8091` 的模拟岗位详情执行自动采集与预填，用于在未取得真实账号前验收字段识别和缺失提示；实验数据无法绕过设备配对或写入真实岗位链路。
33. 岗位采集核对页增加字段来源和质量检查：标记页面采集字段，并在保存前提示必填缺失、薪资/薪数范围异常、过短 JD 与不稳定岗位名称；它只提示和阻止明显无效输入，不会自行篡改 BOSS 可见信息。

普通 HR 的产品信息架构收敛为托管概览、待跟进会话、离开托管、BOSS 账号连接和发送记录。职位、组织、用户、审计和运行保障降级为管理员支撑配置。

## 已裁剪功能

以下历史功能已从当前运行版删除；V2 简历 AI 将依据新的隐私、证据和人工决策边界重新独立实现，不恢复旧代码：

- 自动招聘任务、执行周期与任务调度租约。
- CSV/XLSX 候选人批量导入。
- 旧版 AI 岗位解析和候选人辅助筛选。
- 面试时间协调。
- HR Webhook 通知、签名试运行接收器和相关密钥配置。

历史 Flyway V1–V17 不删除，用于保持已有数据库的校验和升级兼容。裁剪模块的历史表目前保留为不再访问的兼容数据，不做破坏性自动清理。

## 验证状态

- Flyway V1–V18 在 PostgreSQL 17 真实容器中迁移成功；V19 已完成迁移脚本，待 Docker 环境启动后执行真实迁移验证。
- 后端 Java 21 + Maven 编译成功，40/40 测试通过。
- 前端 TypeScript 检查通过；当前环境缺少 `lightningcss` 原生模块，Vite 生产构建待依赖恢复后复验。
- 前端 Vitest 9/9 通过；精简后既有 Playwright 桌面/移动端流程 16/16 通过。
- 浏览器伴随端静态检查与 13/13 单元测试通过；真实列表识别已通过，选中会话消息方向等待 V0.13 实页验证。
- 浏览器设备 API 桌面/移动端 2/2 通过，覆盖配对、心跳、策略读取、会话绑定、消息幂等、撤销和双通道防重。

## 真实 BOSS 账号到位后

1. 已在 HR 手动登录的独立 Chrome Profile 中完成真实会话列表与未读角标适配。
2. 继续只监测，验证 HR 手动选中会话后的最新消息方向、时间和登录/风险状态。
3. 在后台核对“待人工确认”状态和安全草稿，并完成批准、拒绝、人工接管验收。
4. 适配真实输入框后先做“只填写不发送”，最后使用可控会话做单账号小配额试发。
5. 任何验证码、风险提示、登录异常、误识别或投诉都必须立即停机。
