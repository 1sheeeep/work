# DEV_STATE

更新时间：2026-08-30。当前分支：`main`。

## 当前产品方向

真实页面验收已停止 CDP 观测路线，产品改用“每账号独立 Chrome Profile + 最小只读桥接器”。最终范围见 [`PRODUCT_REQUIREMENTS.md`](PRODUCT_REQUIREMENTS.md)。

- 服务对象：公司内部多个 BOSS 招聘账号。
- 核心闭环：离开托管自动接待、平台内简历/联系方式操作、AI 简历辅助分析、HR 简洁工作台。
- 本机方式：每账号独立 Chrome Profile 与桥接器配对，HR 手动登录和处理验证码。
- 安全底线：不保存 Cookie/密码，不调用私有接口，不使用无头浏览器；异常、风险提示、登录失效和结果不确定时暂停。

## 本次已完成

1. 已删除数据库中明确标记的 E2E、Mock、UI 测试企业、账号、岗位、候选人与关联运行数据，保留正式企业、账号和岗位。
2. 正式账号类型由 `BROWSER_COMPANION` 升级为 `LOCAL_CDP_CONNECTOR`；Flyway V30 会自动迁移既有正式账号。
3. 后台账号、托管和岗位页面改为“本地 Chrome 连接器”文案。
4. 新增 [`boss-local-connector`](boss-local-connector/)：支持多账号配置校验、独立 Profile、独立 CDP 端口、手动登录、一次性连接令牌、本地状态保护和安全心跳。
5. 连接器会通过 CDP 只读检查 BOSS 页面、沟通页、登录失效和风险/验证提示；在沟通页稳定时同步会话未读计数、岗位标题和不可逆摘要。若 HR 已手动打开会话，会额外只读复核最后消息方向与时间，驱动现有安全草稿条件；候选人姓名、消息正文、会话 ID、Cookie 和密码均不上传，也不会点击、跳转、填写输入框或发送消息。
6. 新增内部简历登记台账：仅保存事件摘要、来源标签与人工审核状态；原始文件、路径和 BOSS 页面数据不进入当前系统。批准后才允许进入下一阶段 AI 分析。
7. 已接入 OpenAI 简历辅助分析链路：HR 授权后仍需逐次确认，才发送本次粘贴的简历文本；请求设置 `store=false`，本地仅保存 SHA-256 输入摘要、模型版本、结构化证据/追问结果和审计。未配置 `APP_OPENAI_ENABLED`、`OPENAI_API_KEY` 与 `OPENAI_MODEL` 时调用保持关闭。
8. 已补齐 AI 后人工复核：每次成功分析可追加“采纳、修正或未采用”的 HR 说明，反馈不可覆盖且不会改变候选人状态或触发外发动作。
9. 已支持 PDF/DOCX 一次性本机提取：仅接受非加密、无宏、最大 8MB 文件，校验文件签名、PDF 页数和 DOCX 结构后提取文本，文件只进入本机临时处理区，不写入业务数据库或持久卷；提取成功后仍需 HR 确认才会发送文本给 OpenAI。
10. 已接入可选 ClamAV 流式扫描门禁：启用后，风险文件、扫描服务故障和异常返回均会拒绝处理并写审计；扫描服务只运行在 Docker 内部网络，病毒库卷不保存简历原文件。
11. 已接入可选本机 Tesseract 扫描件 OCR：只支持 PNG、JPG/JPEG，强制经过已启用的病毒扫描；识别文本只返回当前 HR 浏览器供核对和修改，提交 OpenAI 前仍需独立确认。
12. 已接入 AI 分析结果保留策略：默认 90 天后自动清除结构化结果、摘要和关联 HR 复核，保留输入摘要和不可变审计；清除后前端明确展示状态，不会误报为分析失败。
13. 已完成第一阶段前端重构：产品名称统一为“招聘值守台”，侧边栏按“工作台、值守与消息、岗位与人才、组织设置、运行管理”分组；首页重构为状态总览、当前优先事项、安全边界、离开托管和最近处理记录。没有真实账号时只显示接入准备，不填充虚构运行数据。
14. 已收敛“账号与浏览器”页：默认不再展示四步配对向导、连接码和过期心跳告警；已保存账号折叠显示，历史设备状态统一为“未运行”。真实账号到位后，管理员才在高级区域打开一次性本机接入设置。
15. 已完成 OpenAI 服务端接入向导：新增仅系统管理员可见的“AI 接入”页、脱敏配置就绪检查和无候选人数据连通性测试；强制官方 HTTPS 端点、`store=false`、安全错误分类和请求 ID 审计。简历分析前端超时已调整为 75 秒，未就绪时禁止提交并引导管理员配置。
16. 已完成旧插件与 Mock 正式链路清理：后端运行包迁移为 `localconnector`，前端、连接器和安全配置统一使用 `/api/local-connector/**`；删除 Mock 网关/账号类型、插件消息同步、人工会话绑定、岗位采集、草稿填写租约与 DOM 发送租约。候选人工作台同步删除手工伪造来信、创建外发草稿和直接审核发送入口，只读展示由真实链路产生的会话记录。V35/V36 已完成数据迁移、旧表字段清理及剩余连接器表、列和索引命名统一。历史 Flyway 文件及可能包含真实记录的孤立面试通知表暂不做破坏性删除。
17. 已完成真实未读岗位标题入库：连接器实时识别并在后端按“企业 + BOSS 账号 + Unicode/大小写/空白/标点规范化标题”去重；V37 增加唯一来源摘要、最后观测时间和观测次数。历史 28 条真实未读观察已回填为 7 个唯一岗位草稿。草稿仅标题可信，补全并由 HR 核对前不能启用，也不会进入岗位知识回复。
18. 已建立统一页面能力矩阵和受控动作队列：V38 持久化七类页面能力及其真实验收状态；已有真实证据只会自动提升未读列表、会话详情和岗位标题三项只读能力。发送消息、索要简历、交换微信和交换电话在没有真实账号验收时保持 `UNVERIFIED`；HR 可登记任务用于后续逐项测试，但任务固定阻断且不存在浏览器领取/执行接口。
19. 已准备真实页面单次人工验收链路：V39 为每台正式连接器生成“发送消息、索要简历、交换微信、交换电话”四张独立验收卡，记录前置条件、预期回执和失败原因；本地连接器新增严格的 `PREVIEW_ONLY` 动作校验契约。没有真实页面时状态固定为 `WAITING_REAL_PAGE`，系统不提供点击器、任务领取或执行接口。
20. 已完成验收状态机和脱敏证据门禁：V40 只接受已配对本地连接器上报的稳定会话摘要与可见操作入口摘要，连续稳定识别至少三次且无验证码/风险提示后才进入 `PREPARED`。管理员可开启 10 分钟人工窗口并登记明确成功或失败；超时、失败和结果不确定都会自动锁定对应能力。单次通过只保持 `READY_FOR_MANUAL_TEST`，绝不会自动升级为生产批准。
21. 已建立受控动作租约与幂等回执框架：V41 只允许运行中的已配对设备为 `PRODUCTION_APPROVED` 能力领取 `READY` 任务，租约一次性且仅 30 秒，令牌仅保存哈希。相同结果与摘要的重复回执安全幂等；冲突回执拒绝覆盖。超时或 `UNKNOWN` 会冻结任务和当前账号，禁止自动重试。连接器当前仅实现协议校验，不轮询租约且没有页面执行器，因此生产写操作仍为零。
22. 已完成生产批准与低配额试运行门禁：V42 要求真实页面验收先通过，再由两位不同管理员依次批准每个账号的单项动作；小时上限 1–5、每日上限 1–20，批准最长 168 小时且自动到期。租约领取会再次实时校验批准有效期和两级额度；撤销、到期或账号紧急停止都会锁定对应写能力。当前没有验收通过记录、生产批准、READY 任务或页面执行器。
23. 已完成本地仿真动作执行器与证据链：V43 记录发送消息、索要简历、交换微信和交换电话四类 `FIXTURE_ONLY` 演练的执行前、执行后和明确回执摘要。离线执行器拒绝任何真实页面适配器和浏览器输入，目标变化、风险状态、回执不明确或状态未变化时立即失败；连续失败会打开本地熔断，且通过一次演练不能自动解除已打开的熔断。演练记录不会提升页面能力、创建生产批准或生成真实动作租约。
24. 已新增一键全套离线故障演练：`npm run drill` 会在不启动 Chrome、不读取账号配置且不写业务数据库的前提下，依次执行消息、简历、微信和电话四条 fixture 状态链，只输出动作、结果和证据来源。任一动作失败即熔断并终止后续动作，避免测试框架掩盖异常。
25. 已完成 V45 故障注入矩阵：`npm run drill:faults` 覆盖目标漂移、风险/验证页、状态未变化、适配器尝试浏览器输入和步骤超时，五类异常都必须失败关闭。每个 inspect/simulate 步骤新增 10–10000ms 有界超时，避免连接器永久挂起；运行保障页显示离线构建清单，并明确不等同于真实页面验收。
26. 已完成 V46 多账号隔离状态机演练：每个账号独立累计瞬时失败，风险/验证、登录失效、目标漂移和未知回执会立即只冻结当前账号；其他账号保持监测。冻结状态不接受健康心跳自动恢复，必须人工确认、页面无风险且连续三次稳定探测后才能恢复到只监测，真实写能力始终保持关闭。`npm run drill:isolation` 会验证三项隔离不变量。
27. 已完成 V47 主循环持久化冻结与人工恢复：真实只读探针发现验证码/风险页或登录失效后，会把当前账号的 `FROZEN` 状态原子写入本机连接器状态文件，后续循环和进程重启都只上报暂停且跳过未读读取。新增 `recover --account ... --confirm-recovery`，要求 HR 明确确认并连续三次识别沟通页后才恢复为只监测；状态文件并发写入已串行化，避免多账号同时异常时互相覆盖。
28. 已完成 V48 上线前综合自检：新增 `preflight` 命令，逐项检查真实执行器关闭、后台传输安全、状态文件权限、Profile/CDP 隔离、配对完整性、账号冻结和 Chrome 就绪提示。报告不包含账号 UUID、设备令牌、Cookie 或页面内容；阻断项存在时退出码为 2。全部通过也只标记可进入真实页面验收，`readyForProduction` 永远为 false。
29. 已完成 V49 旧 ATS 残余清理：前端删除手工新增候选人、手填来源 ID、硬规则、AI 结论、模型和提示版本入口；后端删除对应公开创建接口、请求 DTO 和伪造筛选记录逻辑。移除 `INTERVIEW_INVITE` 能力以及旧招聘任务、候选人批量导入、面试排期和 HR 通知日志文案；部署前只读确认九张旧表均为 0 行，Flyway V44 通过新增迁移删除这些无人使用的表并收紧账号能力约束，历史迁移文件保持不变。
30. 已完成 V50 前端信息架构收敛：左侧导航由 11 个同时平铺入口收敛为“日常工作、消息值守”6 个高频入口，企业、用户、AI、运行与日志归入按权限展开的“管理中心”。“消息值守”默认仅保留值守设置、待审草稿和处理记录三个页签，连接器能力、真实页面验收、租约、生产批准及离线演练转入管理员高级入口；首页完整准备清单默认折叠。后端能力和审计数据未删除。
31. 已开始 V51 真实账号只读验收：正式账号已建立独立 `boss-main-01` Profile 与 54101 本机 CDP 端口，旧离线设备已由一次性令牌安全替换；综合自检无阻断项且生产执行器保持关闭。真实页面确认 `/web/chat/recommend` 为推荐牛人页、`/web/chat/index` 为沟通入口，页面探针已收紧路径匹配，禁止把推荐页或 `/web/chat/job/list` 误判为可监测沟通页。真实沟通页曾稳定识别 43 个会话和 2 个当前未读，仅同步岗位标题和摘要；新增 5 秒有界列表就绪等待、旧设备未读归档和独立消息时间行解析。2026-08-30 再次人工打开已读会话时，页面短暂加载出 64 条可见会话和 1 个选中会话，随后约 1 秒内由 BOSS 页面自身跳转到 `/web/chat/job/list`；双快照稳定性门禁已正确拒绝同步，未覆盖或归档任何历史记录。该跳转原因尚未由真实页面人工确认前，不缩短稳定性等待、不强制导航，也不进行反检测适配。旧设备 28 条历史未读仍需在 HR 能稳定停留 `/web/chat/index` 后由新设备首个成功快照完成归档。
32. 已修复真实账号首个新设备快照暴露的数据库约束遗漏：V45 将 `SOURCE_REPLACED` 纳入未读观察的合法归档状态，后端 69 项测试通过且本地数据库迁移成功。修复后再次验证时，页面探针先确认 `/web/chat/index` 就绪，但进入 CDP 双快照观测期间 BOSS 页面再次跳转到 `/web/chat/recommend`；连接器按页面变化规则暂停且未同步瞬时数据。连续出现平台页面重定向后，真实 CDP 读取暂缓，不反复探测、不强制导航、不缩短稳定性门禁，也不开发反检测绕过；需先确认这是账号页面业务跳转还是与本地观测相关，再决定是否继续 CDP 路线。
33. 已完成 V52 单连接只读会话重构：人工对照确认连接器不运行时 BOSS 沟通页不会自动跳转，因此将原先一次心跳内的页面探针、列表观测和选中会话复核从多次 CDP 连接/断开合并为单次连接；列表与详情共用同一组 800ms 双快照，不缩短稳定性门禁。连接器 40 项测试通过。该改动只减少页面附着次数，不代表已排除平台重定向；真实账号下一次验证必须仍由 HR 在场且仅执行一次，若再次跳转立即暂停 CDP 路线。
34. 已启动 V53 最小只读桥接迁移：新增 [`boss-browser-bridge`](boss-browser-bridge/) Manifest V3 扩展，复用现有一次性配对、心跳、未读观测、会话方向复核和岗位去重接口。它只申请 storage/alarms/tabs 及 BOSS/localhost 站点权限，不申请 cookies、webRequest、debugger 或下载权限；会话、预览和最后消息在页面内先转换为 SHA-256 摘要，只有岗位标题以可读形式进入待核对草稿。当前实现不含点击、输入、跳转、发送、求简历或交换联系方式代码，4 项桥接核心测试已通过。设计只参考 OpenCLI 的 Profile 隔离和 MV3 后台桥接思路，未复制其源码，未引入 boss-cli 私有接口逻辑。
35. 已完成 V54 真实只读桥接首次验收与客户端身份加固：真实 BOSS 沟通页在无 CDP 情况下稳定识别 43 个会话和 34 个未读，新桥接设备在后端为 `ACTIVE/RUNNING`，旧 CDP 设备已撤销。34 条当前未读已入库，对应 7 个当前岗位标题，岗位资料仍按严格规范化标题去重，不会擅自合并可能不同的相似岗位。Flyway V46 为设备新增 `client_type/client_version`，新配对必须声明 `BROWSER_READONLY_BRIDGE` 及合法版本；真实运行接口不再接受 `LEGACY_CDP_CONNECTOR` 设备令牌，管理页会显示桥接类型与版本。
36. 已完成 V55 部分快照安全加固：真实 BOSS 页面后续一次只呈现 9 个当前未读，而后台仍持续观察 34 条历史未读，确认列表存在虚拟滚动、筛选或懒加载造成的非完整快照。后端不再把本次缺席理解为已读，而将其标记为 `SNAPSHOT_CONFIRMATION_REQUIRED`，清除过期会话方向复核并禁止进入草稿审核；重新出现在稳定快照后才恢复正常复核。桥接器分别显示本次页面会话、本次未读和后台观察队列，自动发送继续关闭。

## 下一开发顺序

1. 在一个独立 Chrome Profile 中加载只读桥接器，重新生成一次性接入码完成配对。
2. 由 HR 在场时用真实沟通页验证未读列表、岗位标题去重和选中会话最后消息方向；如出现页面变化或验证提示立即暂停。
3. 验证“未读观测 → 超时判定 → 岗位/企业知识 → 安全草稿 → HR 审核”完整只读链路；自动发送继续关闭。
4. 单账号小配额受控自动接待。
5. 用真实页面验证简历事件、平台内联系方式交换，再接入受控操作。
6. 由公司管理员按 README 完成 OpenAI 服务端 Key 与模型配置，先通过无简历数据连通性测试，再用脱敏测试文本验证分析输出、版本记录与 HR 复核体验；真实简历只在公司已完成隐私授权后使用。
7. 为人工批准的简历事件评估可选加密隔离区与下载审计；当前原文件不留存，AI 结果已有到期清除，OCR 仅可辅助人工核对。
8. 将受控页面操作逐项纳入真实页面验收；未通过验收的发送能力继续固定拒绝执行。

## 验证要求

- 后端测试、前端测试、连接器配置单元测试必须通过。
- Flyway 必须在本地 Docker PostgreSQL 上迁移至 V46。
- 在没有真实账号页面适配通过前，`APP_BROWSER_MONITOR_ONLY` 必须保持 `true`，连接器不实现发送动作。
