# BOSS 多账号超时自动回复控制台实施路线

项目以“每次交付一个可运行、可测试、可审计的真实闭环”为原则。V1 终点是“确认面试时间并通知 HR”，不包含 ATS、Offer 和入职。

## 当前核心需求口径

1. 系统必须支持多个 BOSS 招聘账号，每个账号独立配置超时时长、发送时段、日配额、最小间隔、失败暂停阈值和回复模板。
2. 当 BOSS 招聘端收到候选人新消息，且最新消息仍来自候选人、HR 超过设定时间未回复时，系统应生成草稿或自动回复。
3. 无可用官方消息 API 时，允许使用 Chrome 浏览器伴随端读取 HR 已登录可见页面的 DOM，识别会话、消息方向和时间，并通过可见输入框与发送按钮完成回复。
4. 每个 BOSS 账号使用独立 Chrome Profile 和独立设备配对；任何会话不得由后端 Gateway 与浏览器伴随端重复发送。
5. 发送前必须再次确认最新消息仍来自候选人，且没有人工接管、超出时段/配额/频率限制或出现验证码、风险提示、登录失效、DOM 变化和后端失联。
6. 系统不导出或上传 Cookie/Token/密码，不调用网页私有接口，不伪造指纹，不绕过验证码或风控；无法承诺第三方平台账号绝不被限制。
7. 真实账号上线必须经过“DOM 学习 → 只监测 → 仅草稿 → 单账号小配额试发 → 扩大”的受控流程。

## 每个里程碑的交付步骤

1. 确认角色、状态机、数据归属、失败分支和不做事项。
2. 设计 Flyway 迁移、唯一约束、外键和索引。
3. 实现后端领域对象、服务、API、权限、数据范围和审计。
4. 实现前端列表、筛选、表单、加载/空/错误状态和桌面/移动端。
5. 补充后端单元测试、前端组件测试和 Playwright 真实流程。
6. 执行迁移、构建、测试、桌面/移动端验收和测试数据回收。
7. 更新 `README.md` 和 `DEV_STATE.md`，再进入下一里程碑。

## M1：组织基础控制台

状态：已完成。

- Session 登录、CSRF、集团资料、企业增改查和软停用。
- 企业名称和编码唯一，关键操作可审计。

## M2：HR 用户、角色和企业授权

状态：已完成，V2 迁移和桌面/移动 E2E 已验收。

- 只管理招聘管理员和招聘专员，保护初始系统管理员。
- HR 账号不物理删除，可启停、重置密码、修改角色和企业授权。
- 企业授权是后续账号、职位、任务和候选人的数据边界。

验收：管理员完成 HR 用户全流程；HR 登录后看不到未授权企业和管理员功能；所有变更有审计。

## M3：BOSS 账号与 Capability

状态：已完成，V3 迁移、Mock Gateway、权限/数据范围和桌面/移动 E2E 已验收。

1. 建立 `boss_accounts`：企业归属、显示名称、外部标识、状态、授权状态和最后检查时间。
2. 定义 Capability，例如职位同步、候选人读取、消息发送和面试邀约。
3. 建立 `BossGateway` 接口和 `MockBossGateway`；平台能力优先经 Gateway 集成，无正式消息 API 时可使用独立 Browser Adapter 操作已登录的可见页面，但不接入私有接口或 Cookie 导出。
4. 用户只能查看企业授权内的账号；变更权限由招聘管理员及以上持有。
5. 完成账号列表、新增/编辑、启停、能力检查和异常展示。

验收：Mock 账号可建立、检查能力、停用并按企业隔离；敏感凭据不出现在 API 响应和日志中。

## M4：职位管理

状态：已完成，V4 迁移、状态机、企业/BOSS/Capability 绑定校验和桌面/移动 E2E 已验收。

1. 定义企业、标题、地点、薪资、经验、学历、JD、筛选要求和状态。
2. 职位必须绑定同企业且具备必要 Capability 的 BOSS 账号。
3. 使用草稿、已启用、已关闭状态，不删除已参与任务的职位。

验收：跨企业绑定由后端拒绝，并通过状态迁移、权限、审计和端到端测试。

## M5：自动招聘任务

状态：已从当前运行版裁剪。V5 历史迁移仅为兼容已有数据库保留。

1. 定义职位、BOSS 账号、执行策略、配额、时间窗口、人工审核开关和状态。
2. 使用草稿、待启动、运行中、已暂停、已完成、失败/需人工介入状态机。
3. 外部调用全部经 Gateway，提供幂等键、重试、频率限制和执行追踪。

验收：Mock Gateway 下任务可启动、暂停、恢复和失败重试，不重复处理同一执行项。

## M6：候选人、筛选和会话

状态：已完成，V6 迁移、候选人去重/匿名化、筛选决策追踪、会话幂等与人工审核已通过桌面/移动 E2E 验收。

1. 建立候选人主档、职位接触关系、去重键和隐私保护策略。
2. 将硬性规则和 AI 建议分开；保存模型/提示版本、理由和人工覆盖记录。
3. 会话消息按外部消息 ID 幂等入库，区分候选人、AI、HR 和系统消息。
4. 默认人工审核后发送；自动发送必须可暂停、可阻断、可频控。

验收：候选人不重复建档，AI 决策和发送动作均可追溯且可人工接管。

## M7：面试时间确认与 HR 通知

状态：已从当前运行版裁剪。V7 历史迁移仅为兼容已有数据库保留。

1. 识别面试意向，生成或收集可选时间，所有时间段明确时区。
2. 处理冲突、过期、重复确认和重新约定。
3. 候选人确认后生成面试记录，经 Notification Gateway 通知负责 HR。
4. 通知失败可重试且不重复发送，最终失败进入人工处理列表。

验收：Mock 环境下从候选人沟通到时间确认、HR 收到通知的 V1 端到端链路通过。

## M8：上线前加固

状态：已完成仓库内可实现与验收部分。跨企业/安全头/CSRF 回归、审计防篡改与脱敏、Gateway 弹性、Prometheus、HTTPS 编排、备份恢复演练和回滚手册已验收。真实域名/证书与外部授权需在目标环境完成。

- 跨企业访问安全测试、审计完整性、密钥外置、HTTPS/Secure Cookie、备份恢复。
- Gateway 超时、限流、断路、重试、幂等和人工降级演练。
- 数据保留、删除/匿名化、日志脱敏、监控、发布和回滚清单。

## M9：后台自动调度与任务租约

状态：已随自动招聘任务从当前运行版裁剪；自动回复自身的调度、租约和幂等保护仍保留。

## M10：Excel/CSV 候选人导入和去重

状态：已从当前运行版裁剪；V10/V14 历史迁移仅为数据库兼容保留。

## M11：AI 岗位解析与辅助筛选

状态：已从当前运行版裁剪；自动回复当前使用账号级固定模板。

## M12：真实 HR 通知渠道

状态：已随面试协调从当前运行版裁剪。

## M13：预发布环境和真实 HR 试运行

状态：预发布 Compose 仍保留，原 HR Webhook 接收器已删除；试运行目标已改为 BOSS 浏览器伴随端的 DOM 学习、只监测、草稿和小配额试发。

## M14：多账号候选人超时自动跟进

状态：已完成。每个 BOSS 账号独立配置超时、发送时段、日配额、最小间隔、失败阈值和模板；默认仅生成待审核草稿，管理员可显式开启自动发送。后台 Gateway 通道使用数据库唯一键和可过期租约防止多实例重复回复；账号无发送能力、人工接管、候选人非活跃、最新消息已是外发或超出时段/配额时均不发送；已绑定网页会话由浏览器伴随端承担发送。

验收：桌面/移动端分别验证两个账号的独立配额与超时自动回复，并验证人工接管后跨调度周期不产生回复。在真实 BOSS 账号到位后，还必须验证“候选人最新来信 → HR 超时未回复 → 发送前二次检查 → 网页发送 → 回写系统”真实闭环。

## M15：BOSS 招聘网页伴随端

状态：仓库内通用能力已完成，真实 DOM 待适配。Manifest V3 扩展已实现可配置适配器、超时判定、发送前二次复核、日配额/最小间隔/时段限制、心跳和风险停机。V16 提供了 10 分钟一次性配对令牌、只存摘要的设备令牌、账号级撤销、脱敏会话绑定、心跳和消息幂等同步；扩展的启用、自动发送、配额、时段和模板由后端账号策略统一下发，后端不可达时失败关闭。已绑定网页会话会排除后端 Mock 自动发送，避免双通道重复回复。待合法招聘者测试账号到位后，完成真实 DOM 学习、只监测验证和受控试发，验收后才能进入真实自动回复试运行。

## 待产品确认的决策

- BOSS 平台规则、使用授权、试运行频率和可接受的停机条件。
- 招聘管理员与招聘专员对账号、职位、任务和候选人的具体操作差异。
- 候选人去重规则、个人数据保留期限和删除/匿名化要求。
